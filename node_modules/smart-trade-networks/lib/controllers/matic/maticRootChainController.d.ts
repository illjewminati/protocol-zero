import { BigNumberish, EventFilter, Signer } from "ethers";
import { ProviderOrSigner } from "../../localTypes";
import { IRootChain } from "../../types";
export declare class MaticRootChainController {
    protected _signer: Signer;
    private _rootChainAddress;
    private _rootChainContract?;
    constructor(rootChainAddress: string, signerOrProvider: ProviderOrSigner);
    private _getIRootChainContract;
    /** Getters */
    getContract(): Promise<IRootChain>;
    getLastChildBlock(): Promise<BigNumberish>;
    currentHeaderBlock(): Promise<BigNumberish>;
    onEvent(filter: EventFilter, onContractevent: any): Promise<any>;
}
