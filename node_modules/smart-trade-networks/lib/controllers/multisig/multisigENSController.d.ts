import { ContractTransaction } from "ethers";
import { MultisigENS } from "../../types";
import { ProviderOrSigner } from "../../localTypes";
import { DecodedCall } from "../helpers/abiDecoderController";
export declare class MultisigENSController {
    private _signer;
    private _multisigENSAddress;
    private _multisigENSContract?;
    constructor(multisigAddress: string, signerOrProvider: ProviderOrSigner);
    private _getMultisigENSContract;
    /**Getters */
    getContract(): Promise<MultisigENS>;
    getMultisigDeployerAddress(): Promise<string>;
    getMultisigName(msigAddress: string): Promise<string>;
    getUserName(userAddress: string): Promise<string>;
    /**Setters */
    setMultisigDeployerAddress(address: string): Promise<ContractTransaction>;
    setUserName(name: string): Promise<ContractTransaction>;
    /** Helpers */
    static encodeCall(functionName: string, args?: any[]): string;
    static decodeCall(data: string): DecodedCall | null;
}
