import { Signer } from "ethers";
import { ProviderOrSigner } from "../../localTypes";
import { RootChainManagerStorage } from "../../types";
export declare class MaticRootChainManagerController {
    protected _signer: Signer;
    private _rootChainManagerAddress;
    private _rootChainmanagerContract?;
    constructor(rootChainAddress: string, signerOrProvider: ProviderOrSigner);
    private _getRootChainManagerContract;
    /** Getters */
    getContract(): Promise<RootChainManagerStorage>;
    getChildToken(rootTokenAddress: string): Promise<string>;
    getRootToken(childTokenAddress: string): Promise<string>;
}
