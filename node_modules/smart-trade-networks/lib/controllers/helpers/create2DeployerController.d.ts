import { ContractTransaction } from "ethers";
import { Create2Deployer } from "../../types";
import { ProviderOrSigner } from "../../localTypes";
export declare class Create2DeployerController {
    private _signer;
    private _create2DeployerAddress;
    private _create2DeployerContract?;
    constructor(create2DeployerAddress: string, signerOrProvider: ProviderOrSigner);
    private _getCreate2DeployerContract;
    /**Getters */
    getContract(): Promise<Create2Deployer>;
    precomputeAddress(contractBytecode: string, salt: string | number, constructorTypes: Array<any>, constructorArgs: Array<any>): string;
    /**Setters */
    deployContract(salt: number | string, contractBytecode: string, constructorTypes: Array<string>, constructorArgs: Array<any>): Promise<ContractTransaction>;
}
