import { DecodedCall } from '../controllers/helpers/abiDecoderController';
export declare function encode(compilerOutput: any, functionName: string, args?: any[]): string;
export declare function decode(compilerOutput: any, data: string): DecodedCall | null;
