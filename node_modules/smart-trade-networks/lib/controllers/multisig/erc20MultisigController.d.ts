import { BigNumberish } from "ethers";
import { ERC20MultisigWallet } from "../../types";
import { ProviderOrSigner } from "../../localTypes";
import { DecodedCall } from "../helpers/abiDecoderController";
import { MultisigController } from "./multisigController";
export declare class ERC20MultisigController extends MultisigController {
    private _erc20MultisigAddress;
    private _erc20MultisigContract?;
    constructor(multisigAddress: string, signerOrProvider: ProviderOrSigner);
    private _getERC20MultisigContract;
    /**Getters */
    getContract(): Promise<ERC20MultisigWallet>;
    getFee(): Promise<BigNumberish>;
    getFeeCollector(): Promise<string>;
    getWithdrawWithFee(amount: BigNumberish): Promise<[BigNumberish, BigNumberish]>;
    /** Helpers */
    static encodeCall(functionName: string, args?: any[]): string;
    static decodeCall(data: string): DecodedCall | null;
}
