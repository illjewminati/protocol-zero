import { BigNumberish, BytesLike, ContractTransaction } from "ethers";
import { Company } from "../../types";
import { ProviderOrSigner } from "../../localTypes";
declare type CompanyData = [string, string, BigNumberish, string] & {
    name: string;
    ipfsLink: string;
    createdAt: BigNumberish;
    owner: string;
};
export declare class CompanyController {
    private _companyAddress;
    private _signer;
    private _companyContract?;
    constructor(companyAddress: string, signerOrProvider: ProviderOrSigner);
    private _getCompanyContract;
    /**Getters */
    getContract(): Promise<Company>;
    getData(): Promise<CompanyData>;
    /** Setters */
    changeName(name: string): Promise<ContractTransaction>;
    changeIpfsLink(ipfsLink: BytesLike): Promise<ContractTransaction>;
}
export {};
