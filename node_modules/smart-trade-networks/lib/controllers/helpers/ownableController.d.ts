import { ContractTransaction, Signer } from "ethers";
import { ProviderOrSigner } from "../../localTypes";
export declare class OwnableController {
    protected _signer: Signer;
    private _address;
    private _contract?;
    constructor(address: string, signerOrProvider: ProviderOrSigner);
    private _getContract;
    /**Setters */
    transferOwnership(newOwner: string): Promise<ContractTransaction>;
}
