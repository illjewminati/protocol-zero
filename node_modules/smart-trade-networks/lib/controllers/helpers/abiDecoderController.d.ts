export interface DecodedParams {
    name: string;
    type: string;
    value: any;
}
export interface DecodedCall {
    name: string;
    params: Array<DecodedParams>;
}
export declare class MultisigAbiDecoder {
    static decodeCall(data: string): DecodedCall | null;
}
